#ifndef BINGO_HPP
#define BINGO_HPP

namespace bingo {
// Enum at namespace scope
enum Choice { Bing, Bang, Bong };
// Function at namespace scope
void run_bingo(Choice choice);
}  // namespace bingo

#endif
